databases:
  - name: ratehive
    databaseName: ratehive_fb0b
    user: ratehive

services:
  - type: web
    name: ratehive
    env: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: ratehive
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false